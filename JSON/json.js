const json = '{ "name" : "shuvo", "hobby" : "photography", "aim" : "nothing"}';

console.log(json); // This one id json formate

const myObj = JSON.parse(json); // parse is making object or whatever in json formate. json to obj

console.log(myObj); // This is not json this is now a valid object